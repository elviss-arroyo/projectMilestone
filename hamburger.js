// Toggle the navigation menu on mobile/tablet
function toggleMenu() {
  const menu = document.getElementById("myLinks"); // looks for menu on page
  menu.classList.toggle("show"); // turns menu on or off 
}
